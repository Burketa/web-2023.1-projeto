<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark px-3">
        <a class="navbar-brand" href="/">{{app.name}}</a>
        <div class=" navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="{{details.url}}">{{details.link}}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{tools.url}}">{{tools.link}}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{developer.url}}">{{developer.link}}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{contact.url}}">{{contact.link}}</a>
                </li>
            </ul>
        </div>
    </nav>
</header>